# - all fields are mandatory
input Commerce_AddressInput {
    firstname: String!
    lastname: String!
    email: String!
    telephone: String!
}

extend type Mutation {
    Commerce_UpdateBillingAddress(address: Commerce_AddressInput): Commerce_CartAddress!
}